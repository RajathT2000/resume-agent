services:
  - type: web
    name: rajath-ai-avatar
    runtime: python-3.11
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: python app.py
    plan: free
    region: oregon
    envVars:
      - key: OPENAI_API_KEY
        sync: false
